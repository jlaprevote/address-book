<h1>Contacts</h1>

<p>Sort by:
  <%= link_to "Last Name A-Z", contacts_path(order: "l_name_asc") %>
  <%= link_to "First Name A-Z", contacts_path(order: "f_name_asc") %>
  <%= link_to "Last Name Z-A", contacts_path(order: "l_name_des") %>
  <%= link_to "First Name Z-A", contacts_path(order: "f_name_des") %>
</p>

<ul>
  <% @contacts.each do |c| %>
  <%= link_to c.f_name, contact_path(c) %>
  <%= link_to c.l_name, contact_path(c) %><br>
  <% end %>

  <br><%= link_to "Create a New Contact", new_contact_path %>
